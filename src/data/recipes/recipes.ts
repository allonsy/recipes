import { Recipe } from "../../models/recipe";
import r1 from "./R-XQijUo-ujP98U80t50A";

export const allRecipes: Recipe[] = [r1];
export const recipeMap: Record<string, Recipe> = {
  [r1.id]: r1,
};
